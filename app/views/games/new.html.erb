<div class="container">
  <h2>Ajouter un jeu</h2>
  <%= simple_form_for @game do |f| %>
    <%= f.input :name, label: 'Nom' %>
    <%= f.input :description, label: 'Description' %>
    <%= f.input :number_of_players, label: 'Nombre de joueurs' %>
    <%= f.input :duration, label: 'Temps pour une partie' %>
    <%= f.input :price, label: 'Prix pour une soirée de location' %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Ajouter", class: "btn btn-primary" %>
  <% end %>
</div>