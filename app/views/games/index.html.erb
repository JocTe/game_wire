<div class="container">
  <h2 class="text-center my-4">A quoi voulez-vous jouer ?</h2>

  <div class="row row-cols-3">    
      <% @games.each do |game| %>
        <div class="card mb-3 col">
          <img src="https://source.unsplash.com/1600x900/?game" class="card-img-top" alt="game picture">
          <div class="card-body">
            <h5 class="card-title"><%= game.name %></h5>
            <p class="card-text"><%= game.description.truncate(200) %></p>
            <%= link_to "Découvrir", game_path(game), class: "btn btn-primary" %>
          </div>
        </div>
      <% end %>    
  </div>
</div>
  


